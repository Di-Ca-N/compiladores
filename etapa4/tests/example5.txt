func1 = param1 <- int | param2 <- float > int {
    int a, b <= 2;
    float c <= 3.1, d;

    {
        int e;

        if (e == 2) {
            float f <= 2.4;
            float e;
            e = f + 1;
        } else {
            float g <= 3.2;
            e = g - 1;
        };
    };
}
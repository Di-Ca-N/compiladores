funcao1 = param1 <- int | param2 <- float > int { // Função
    int a, b, c<=2;           // Declaracao de Variavel
    float d <= 1.1, e;

    if (expr + !(5 - 4 % a > - d & e | 2.4)) {
        a = b * c;
    };

    if (expr) {
        funcao2(a,b,e);
    } else {
        funcao3(c,d,e);
    };

    while (expr) {
        funcao4(expr);
    };
    
    {
        {
            a = 2;
        };
    };
    return expr;
}

funcao1 = param1 <- int | param2 <- float > int { // Função
    int a, c<=2, b, d <= 4;           // Declaracao de Variavel
    float d <= 1.1, e;
    int f, g;

    if (expr + !(5 - 4 % a > - d & e | 2.4)) {
        a = b * c;
    };

    if (expr) {
        funcao2(a,b,e);
        funcao4(a,e,f);
    } else {
        funcao3(c,d,e);
    };

    while (expr) {
        funcao4(expr * 2);
    };
    
    {
        {
            a = 2;
        };
    };
    {};
    {};
    a = 1;
    return expr;
}

funcao2 = > int {
    funcao1(1, 2.2);
    return 0;
}